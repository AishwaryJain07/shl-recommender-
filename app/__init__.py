"""SHL Conversational Assessment Recommender — application package.

See BUILD_SPEC.md for the source-of-truth design. Phase 0 ships `config.py`
and `catalog.py`; later phases add retrieval, agent policy, LLM client, and the
FastAPI service.
"""
